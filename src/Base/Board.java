package Base;

import Entitys.Entity;

public class Board {
    
    GamePanel gp;

    public Board(GamePanel gp)
    {
        this.gp = gp;
    }

    /**
     * boards up the closest window to the player
     * @param entity
     */
    public void BoardWindow(Entity entity)
    {
        
    }
}
